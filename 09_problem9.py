# Can you change the values inside a list which is contained in set S?
s = {8, 7, 12, "Moattar", [1,2]}

# Sets only allow immutable and hashable elements.
# Lists are mutable, so they cannot be added to a set.





